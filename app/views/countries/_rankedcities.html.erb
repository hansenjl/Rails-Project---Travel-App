<li><strong><%= link_to city.name, city_path(city) %>,  <%= link_to city.country.name, country_path(city.country) %></strong>  |  overall rating: <%= city.overall_rating %>  |  visitors: <%= city.times_visited %></li>